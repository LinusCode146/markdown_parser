# Überschrift 1

Dies ist ein normaler Paragraph mit **fett** und *kursiv* und `inline code`.

## Überschrift 2

Ein Paragraph mit __fett__ und _kursiv_ kombiniert.

### Überschrift 3

Hier ist ***fett und kursiv*** zusammen in einem Text.

#### Überschrift 4

Ein weiterer Paragraph mit mehreren **fetten Wörtern** und *kursiven Wörtern* im selben Satz.

##### Überschrift 5

Dies ist ein komplexer Paragraph mit **fett**, *kursiv*, ***fett und kursiv***, und `code` gemischt.

# Test für Überschrift mit **Formatierung**

## Überschrift mit *kursivem* Text

### Überschrift mit `Code`

Ein Paragraph nur mit **fett**.

Ein Paragraph nur mit *kursiv*.

Ein Paragraph nur mit `inline code`.

Ein Paragraph mit __Unterstrichen für fett__ und _Unterstrichen für kursiv_.

Dies ist ein langer Text mit vielen **fetten Wörtern** und *kursiven Wörtern* und `code snippets` wie `console.log()` und noch mehr **fett** und *kursiv* Text.

# Edge Cases

**Dies ist der gesamte Paragraph fett**

*Dies ist der gesamte Paragraph kursiv*

`Dies ist der gesamte Paragraph code`

***Der gesamte Paragraph ist fett und kursiv***

Mehrere **fette** Wörter in **einer** Zeile.

Mehrere *kursive* Wörter in *einer* Zeile.

Mehrere `code` snippets in `einer` Zeile.

**Fett am Anfang** normal in der Mitte *kursiv am Ende*

# Kombinationen

**Fett mit `code` darin** und normal.

*Kursiv mit `code` darin* und normal.

`Code mit **fett** darin` (könnte problematisch sein).

# Leerzeichen und Sonderzeichen

**fett**ohne**Leerzeichen**

*kursiv*ohne*Leerzeichen*

Normal **fett** normal **fett** normal.

# Satzzeichen

**Fett!** und *Kursiv?* und `Code.`

Dies ist ein Satz mit **Fett,** *Kursiv;* und `Code:` Satzzeichen.

# Zahlen und Buchstaben

**123** und *abc* und `x = 5`

Die Variable `x` ist **wichtig** und *sehr* relevant.

# Leere Zeile dazwischen

Erster Paragraph.

Zweiter Paragraph mit **Formatierung**.

# Unicode und Umlaute

**Äpfel** und *Öl* und `ÜbungsmäßÄßig`

Dies ist ein **deutscher** Text mit *Umlauten* wie `äöüÄÖÜß`.

# Sehr langer Text

Dies ist ein sehr langer Paragraph mit vielen **verschiedenen** *Formatierungen* und `Code-Snippets` um zu testen wie die Implementation mit längeren Texten umgeht. Es gibt **mehrere fette Wörter**, *mehrere kursive Wörter*, und `mehrere code snippets` in diesem einen Paragraph um sicherzustellen dass alles korrekt funktioniert.



